<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('partials/head') %>
        <style>
            *,
            *::before,
            *::after {
                margin: 0;
                padding: 0;
                box-sizing: border-box;
            }

            body {
                font-family: 'Arial', sans-serif;
                background-color: #f4f4f9;
                color: #333;
                line-height: 1.6;
            }

            .edit {
                background-color: #007bff;
                color: white;
                border: none;
                font-size: 17px;
                padding: 10px 10px;
                text-decoration: none;
                border-radius: 4px;
                cursor: pointer;

            }


            .container {
                width: 90%;
                max-width: 1200px;
                display: flex;
                flex-direction: column;
                margin: 20px auto;
                padding: 20px;
                background-color: white;
                border-radius: 8px;
                box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            }

            .my_style {
                margin-bottom: 10px;
                padding: 10px 5px;
                padding: 10px;
                font-size: 1rem;
                border: 1px solid #ccc;
                border-radius: 4px;
            }

            footer {
                font-size: 0.9rem;
                text-align: center;
            }
        </style>
</head>

<body>
    <%- include('partials/header') %>

        <form class="container" action="/edit/<%= todo.id %>" method="POST">
            <h1>Edit Task</h1>
            <input type="text" class="my_style" name="task" value="<%= todo.task %>" required>
            <select class="my_style" name="priority">
                <option value="High" <%=todo.priority==='High' ? 'selected' : '' %>>High</option>
                <option value="Medium" <%=todo.priority==='Medium' ? 'selected' : '' %>>Medium</option>
                <option value="Low" <%=todo.priority==='Low' ? 'selected' : '' %>>Low</option>
            </select>
            <button class="edit" type="submit">Update Task</button>
        </form>

        <%- include('partials/footer') %>
</body>

</html>